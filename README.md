# railway
The Railway Waiting List Queue is a Java-based console application that simulates the real-world behavior of a railway reservation waiting list system. It uses a Queue data structure (FIFO – First In, First Out) implemented through Java’s LinkedList to manage passengers efficiently.
